Dear <%= @user.name %>,

Public Lab contributor <%= @node.author.name %> (http://publiclab.org/profile/<%= @node.author.name %>) just posted a new research note entitled '<%= @node.title %>':

Read and respond to the post here: http://publiclab.org<%= @node.path %> 

-------------------------

<%= @node.body %>

=========================

You received this email because you are subscribed to the following tags: <%= @tags.to_a.collect(&:name).join(",") %>.

To change your preferences, please visit http://publiclab.org/subscriptions.

