<div class="col-md-2">
  <%= render :partial => "search/nav", locals: { section: "search-all" } %>
</div>

<div class="col-md-10">

  <div id="google" id="gcse-searchresults-only" resultsUrl="https://publiclab.org/wiki/search" ></div>

  <div class="container">
    <% if  @nodes.all?{|k,v| v.blank?} %>
      <h4>
        No Content found for <%= params[:query] %> :-(
        </br></br>
        Try searching <a href="/search">for another topic</a>
      </h4>
    <% else %>
      <h2> Results for <%= params[:query] %></h2>
      <% unless @notes.blank? %>
        <h3> NOTES</h3>
        <table class="table inline-grid">
          <tr>
              <th>Title</th>
              <th> Posted by </th>
              <th> Views</th>
          </tr>

          <% @notes.each_with_index do |note, index| %>
            <% if index < 9 %>
              <tr>
                <td style="width:40%"><a href=<%= "#{note.path}" %>><%= note.title %></td>
                <td><%=note.author.name%></td>
                <td><%= note.views%></td>
              </tr>
            <% end %>
          <% end %>

          <% if (@notes.count > 9) %>
            <tr>
              <td>
                <b><%= link_to "More matching notes >>", "/search/notes/#{params[:query]}" %></b>
              </td>
            </tr>
          <% end %>

        </table>
      <% end %>
    <% end %><br>
          
    <%= render partial: 'wikis_blank' %><br>
    <%= render partial: 'questions_blank' %><br>
    <%= render partial: 'profiles_blank' %><br>
    <%= render partial: 'tags_blank' %>
  </div>
</div>

<script>

var url_name = window.location.pathname;
query_data = url_name.split('/')[2];


var search, onSearchReady = function() {
  if (!search) search = google.search.cse.element.render({div:'google'});
  console.log(search)
}

window.__gcse = {
  //parsetags: 'explicit',
  callback: onSearchReady
};

(function() {
  var cx = '008366342703784425914:_p25pf-gy_w';
  var gcse = document.createElement('script'); gcse.type = 'text/javascript';
  gcse.async = true;
  gcse.src = '//cse.google.com/cse.js?cx=' + cx;
  var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(gcse, s);
})();

</script>