<% if @node.has_mailing_list? %>
<div class='well'>
  <% mailing_list = @node.power_tag("list") %>

  <form action="https://groups.google.com/group/<%= mailing_list %>/boxsubscribe">
    <label>Join the <a href="https://groups.google.com/group/<%= mailing_list %>"><b><%= mailing_list %></b> mailing list</a>:</label>
    <div class="input-group">
      <span class="add-on"><i class="icon-envelope"></i></span><input name="email" class="col-md-11" type="text" placeholder="you@email.com"/>
    </div>
    <br />
    <button name="sub" type="submit" class="btn btn-primary">Subscribe</button>
  </form>

  <hr />
  <h4>Recent discussion topics</h4>

  <ul class="nav bullet">
  <% @node.mailing_list[0..4].each do |item| %>
    <li>
      <a href="<%= item.link %>"><i class="icon-comment"></i> <%= item.title.gsub('Re: ','').gsub('['+mailing_list+']','') %></a>
      <p><small><i><%= time_ago_in_words(item.pubDate) %> ago</i></small></p>
    </li>
  <% end %>
  </ul>
  <p><a href="https://groups.google.com/group/<%= mailing_list %>">All topics &raquo;</a></p>
</div>
<% end %>
