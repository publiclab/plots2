<div class="span3">
  <%= render :partial => "sidebar/post_button" %>

  <hr />
  <p><i class="icon icon-envelope"></i> <a href="/wiki/mailing-lists">Mailing lists &raquo;</a></p>
  <p>Much of our work happens on our mailing lists, which are organized by region and topic. Join one today to get involved.</p>
  <hr />

  <% if @wikis && @wikis.length > 0 %>
  <div class="hidden-phone">
  <h4>Related wiki pages</h4>
  <ul class="nav wikis bullet">
    <% @wikis.each do |wiki| %>
      <li><a href="<%= wiki.path %>"><i class="icon-book"></i> <%= wiki.title %></a> <small style="color:#888;">(updated <%= time_ago_in_words(wiki.updated_at) %> ago)</small></li>
    <% end %>
  </ul>
  <% end %>
  </div>

</div>
