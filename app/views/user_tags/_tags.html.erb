<% @tags.each do |tag| %>
  <span id="tag_<%= tag.id %>" class="label label-primary" style="font-size:13px;margin-right: 4px;">
    <%= tag.value.split(":")[1] %>
    <a class="tagdelete" data-remote="true" href="/tag/delete/<%= tag.id %>">x</a>
  </span>
<% end %>
