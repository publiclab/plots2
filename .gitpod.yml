ports:
  - port: 3000
    onOpen: open-preview
tasks:
  - init: >
      rvm install 2.4.4
      bundle install --without production &&
      rails db:migrate
    command: >
      rails server
github:
    prebuilds:
        pullRequestsFromForks: true
